import styled from 'styled-components';

export const Screen = styled.div`
    width: 100%;
    min-height: 100vh;
    background: rgb(51,51,51);
    background: linear-gradient(145deg, rgba(51,51,51,1) 0%, rgba(34,34,34,1) 100%);

    display: flex;
    flex-direction: column;
    align-items: center;
`;